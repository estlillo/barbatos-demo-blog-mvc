<div class="relative mb-6 w-full">
    <flux:heading size="xl" level="1">{{ $title ?? __('Título por defecto') }}</flux:heading>
    <flux:subheading size="lg" class="mb-6">{{ $subtitle ?? '' }}</flux:subheading>
    <flux:separator variant="subtle" />
</div>
